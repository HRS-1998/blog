### 小程序的嵌套方案

### 性能优化

1. `uni.onMemoryWarning(callback)` 与 `uni.offMemoryWarning(callback)` 内存监听
   当 iOS/Android 向小程序进程发出内存警告时，触发该事件。Android 下有告警等级划分，iOS 无等级划分。

```
const callback = function (res) {
 console.log(res,'onMemoryWarningReceive');
}
uni.onMemoryWarning(callback);
```

2. 长列表 上拉加载下拉刷新实现
[mescroll](https://ext.dcloud.net.cn/plugin?id=343)
